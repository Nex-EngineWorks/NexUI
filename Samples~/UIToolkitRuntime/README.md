# NexUI UI Toolkit Runtime Sample

Runtime UI Toolkit sample for HUD, Inventory, PauseMenu, Toast, state binding, motion, and theme switching.

Use a scene object with `UIDocument`, `UIToolkitIntegrationBootstrap`, and `UIToolkitRuntimeDemo`. Assign `UIScreenDefinition` assets that point to UXML backend assets.
