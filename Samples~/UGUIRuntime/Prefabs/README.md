# Prefabs

Place uGUI prefabs for `HUD`, `PauseMenu`, and reusable controls here. Add NexUI uGUI capability components such as `NXButtonUGUI`, `NXLabelUGUI`, and `NXProgressBarUGUI` to bind elements.
