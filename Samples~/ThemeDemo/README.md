# NexUI Theme Demo

This sample demonstrates theme switching, runtime token overrides, responsive rule application, and component variants.

## Controls

- `T`: switch between assigned light/dark themes
- `R`: apply a responsive token override based on the current screen width
- `C`: clear runtime overrides

Backends apply token values through their registered `IUIThemeApplier`.
